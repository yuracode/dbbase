# 第0回：なぜ3種類のデータベースを学ぶのか

## 授業情報

| 項目 | 内容 |
|------|------|
| 前提知識 | なし（このシリーズの出発点） |
| 動作環境 | なし（座学・概念理解） |
| 到達目標 | MariaDB / MongoDB / Redis それぞれを「なぜ使うか」を説明できる |

---

## このシリーズで学ぶこと

このシリーズでは **3種類のデータベース** を順に学ぶ。

```
第1回  MariaDB  ── リレーショナルDB（RDBMS）
第2回  MongoDB  ── ドキュメント型 NoSQL
第3回  Redis    ── キーバリュー型 NoSQL（インメモリ）
```

「なぜ1種類じゃダメなのか」──それがこの回のテーマ。

---

## データベースとは何か（30秒おさらい）

**データベース** = データを永続的に保存・検索・更新できる仕組み。

ファイルに保存するのとの違いは主に3点：

| 比較 | ファイル | データベース |
|------|---------|------------|
| 検索 | 全部読んで探す | インデックスで高速検索 |
| 同時アクセス | 壊れやすい | 排他制御で安全 |
| 整合性 | 自前で管理 | DBが保証 |

---

## 第1回：MariaDB（RDBMS）── 本当はどれを使うべきか？

### RDBMSとは

**RDBMS（Relational Database Management System）** は、データを **表（テーブル）** で管理するデータベース。
SQL という標準言語で操作し、複数テーブルを結合して複雑な検索もできる。

### PostgreSQL でよくない？

正直なところ、RDBMSを本格的に学ぶなら **PostgreSQL** の方が「正解」に近い。

| 比較軸 | PostgreSQL | MariaDB（MySQL互換） |
|--------|-----------|-------------------|
| SQL標準への準拠 | 非常に高い | おおむね準拠（独自拡張あり） |
| JSON サポート | JSONB型で高機能（インデックスも張れる） | JSON型はあるが機能は限定的 |
| ウィンドウ関数・CTE | 完全サポート | バージョンによる制限あり |
| トランザクション分離 | 全レベル対応 | 一部制限あり |
| 学習コスト | やや高め | 低い（シンプル） |
| 用途の広がり | 本番システム・研究用途で強い | Webアプリ・共有ホスティング |
| MySQLとの互換性 | なし | ほぼ互換（drop-in replacement） |

**→ 今回あえてMariaDBを選んだ理由：**

- MySQL との互換性が高く、現場で見かける機会が多い
- `apt install` だけで動き、設定がシンプル
- SQL の基礎構文は PostgreSQL とほぼ同じなので、後から PostgreSQL に移っても困らない
- WSL2上での初学者向け環境として取り組みやすい

> **まとめ：** MariaDB で学んだ SQL（CREATE TABLE / INSERT / SELECT / UPDATE / DELETE）は PostgreSQL でもそのまま使える。構文の違いは細部だけ。「まずSQLを体に覚えさせる」目的ではMariaDBで十分。

---

## 第2回：MongoDB を学ぶ意味

### RDBMSだけでは困る場面がある

RDBMSは「テーブルの構造（スキーマ）を事前に定義する」必要がある。
しかし現実のシステムではこれが足かせになることがある。

**例1：SNSの投稿データ**
```
テキスト投稿   → { text, user, created_at }
画像投稿       → { images[], user, created_at, location }
動画投稿       → { video_url, duration, user, created_at, tags[] }
```
投稿ごとにフィールドが違う。RDBMSで管理しようとすると、使わない列だらけのテーブルか、テーブルを大量に分けるかになる。

**例2：IoT センサーデータ**

センサーの種類が増えるたびにテーブル定義を変更（`ALTER TABLE`）するのは、大量データが入った本番DBでは危険でコストも高い。

**例3：外部APIのレスポンスをそのまま保存したい**

JSON を受け取って、変換せずにそのまま保存→後から検索する、という用途にRDBMSは向かない。

### MongoDB が解決すること

MongoDB はスキーマを事前に定義しない **ドキュメント型NoSQL**。

```javascript
// こういうドキュメントを、スキーマ変更なしに同じコレクションに混在して保存できる
{ user: "alice", text: "こんにちは" }
{ user: "bob",   images: ["cat.jpg"], location: "Tokyo", tags: ["cat", "photo"] }
```

| MongoDB を学ぶ意義 |
|--------------------|
| 「スキーマレス」という設計思想を理解できる |
| 実際のWebアプリ開発で頻繁に登場する（Node.js + MongoDB は定番構成） |
| JSON形式のデータを直感的に扱うスキルが身につく |
| RDBMSとNoSQLの使い分けを判断できるようになる |

> **向いていない用途も理解しておく：** 複数コレクションをまたぐ複雑な結合や、厳密な金融トランザクションには向かない。この「使い分け」を知ることが学習のゴール。

---

## 第3回：Redis を学ぶ意味

### 「速さ」が設計の中心にあるDB

MariaDB も MongoDB も、データを **ディスク（SSD/HDD）** に保存する。
Redis は違う。データを **メモリ（RAM）** に保持する。

```
ディスクアクセス ： マイクロ秒〜ミリ秒
メモリアクセス   ： ナノ秒〜マイクロ秒
                                ↑ 100〜1000倍速い
```

### 実際のWebシステムでRedisが担う役割

**シナリオ：1万人が同時にログインしているWebサービス**

```
① ユーザーがページを開く
② サーバーが「このユーザーはログイン済みか」を確認したい

   方法A（Redisなし） → MySQLにSELECTクエリ → 毎回ディスクアクセス → 遅い
   方法B（Redisあり） → Redis に「session:user123」があるか確認 → メモリアクセス → 高速
```

**Redis が現場で使われる主な場面：**

| 用途 | 仕組み |
|------|--------|
| セッション管理 | ログイン状態を Redis に保存。TTL（有効期限）で自動ログアウト |
| キャッシュ | 重いSQLの結果を Redis に保存。次のリクエストはDBを叩かない |
| リアルタイムランキング | Sorted Set でスコアを管理。順位の更新・取得が O(log N) で高速 |
| カウンター | いいね数・アクセス数を `INCR` で原子的にカウントアップ |
| タスクキュー | List を使ってジョブをキューイングし、ワーカーが順番に処理 |

| Redis を学ぶ意義 |
|-----------------|
| インメモリDBという全く異なるアーキテクチャに触れられる |
| TTL という「データに有効期限をつける」発想を体験できる |
| MariaDB + Redis の組み合わせは現場で最も見かける構成の一つ |
| キャッシュという概念を「手を動かして」理解できる |

> **注意点：** Redisは基本的に電源を切るとデータが消える（永続化オプションはある）。「一時的に高速に扱いたいデータ」がRedisの領域。長期保存は MariaDB / MongoDB の役割。

---

## 3つを組み合わせると何ができるか

実際の Webシステムでは、複数のデータベースを役割分担して使う。

```
【 典型的な構成例：ECサイト 】

ユーザー情報・注文履歴・商品カタログ
  └─→ MariaDB（RDBMS）
        構造が決まっていて、整合性が重要なデータ

商品レビュー・ユーザー行動ログ
  └─→ MongoDB（ドキュメント型NoSQL）
        フィールドが可変で、大量書き込みが必要なデータ

セッション・カート（一時データ）・ランキング
  └─→ Redis（インメモリ）
        高速アクセスが必要な一時的・集計的なデータ
```

1種類しか知らないと「すべてをRDBMSで解こうとする」か「流行りのNoSQLを無闇に使う」かになりがち。
**適材適所の判断力** がこのシリーズの最終的なゴール。

---

## 学習の進め方

```
第0回（今回）  ── なぜ3種類学ぶかを理解する  ← ここ
第1回          ── MariaDB でSQLの基本を身につける
第2回          ── MongoDB でスキーマレスの感覚をつかむ（第1回完了後）
第3回          ── Redis でインメモリ・TTLを体験する（第1〜2回完了後）
```

> 第2回・第3回では「第1回のRDBMSと何が違うか」を常に意識しながら進めると理解が深まる。

---

## まとめ

| DB | 種別 | キーワード | 代わりになれる？ |
|----|------|-----------|----------------|
| MariaDB | RDBMS | テーブル・SQL・整合性・トランザクション | PostgreSQL でも可（SQL互換） |
| MongoDB | ドキュメント型NoSQL | スキーマレス・JSON・柔軟な構造 | RDBMSでは代替しにくい設計思想 |
| Redis | キーバリュー型NoSQL（インメモリ） | 高速・TTL・キャッシュ・一時データ | 他DBでは速度面で代替困難 |

---

## 次回予告

第1回では **MariaDB** をインストールし、SQL でテーブルの作成・データ操作を体験します。
「なぜこのコマンドを書くのか」をこの第0回で理解した上で取り組むと、格段に定着が速くなります。
